import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

describe('<Month />', () => {
  it('sets the selected prop on the Day component to true', () => {

  });

  it('sets the selected prop on the Day component to false', () => {

  });
});
